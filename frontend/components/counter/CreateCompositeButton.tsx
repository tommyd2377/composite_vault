"use client";

import React from "react";

export function CreateCompositeButton(): React.JSX.Element | null {
  // Component intentionally removed the visible "Create Composite" UI.
  // Returning null prevents the button from being rendered in the frontend.
  return null;
}
